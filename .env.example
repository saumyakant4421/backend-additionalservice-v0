# Example environment variables for local development
# Do NOT commit your real service account JSON into source control.
# For production, use Secret Manager or a runtime service account with appropriate IAM roles.

# Optional: full service account JSON (as a single-line JSON string). Not recommended for production.
# FIREBASE_SERVICE_ACCOUNT={...}

# Optional: path to local service account JSON file
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-adminsdk.json

# Optional: explicit GOOGLE_APPLICATION_CREDENTIALS path
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\sa-key.json

# If set to 'true', SKIP_SECRET_MANAGER prevents attempting Secret Manager.
# Useful in local dev when you don't have access to the GCP project secrets.
# SKIP_SECRET_MANAGER=true

# App port
PORT=4004

# TMDB API key (required for movie searches)
TMDB_API_KEY=your_tmdb_api_key_here
